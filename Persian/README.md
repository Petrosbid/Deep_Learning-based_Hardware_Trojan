# **DeepNet-Trojan: یک خط لوله یادگیری عمیق و پردازش زبان طبیعی برای تشخیص تروجان سخت‌افزاری**

این مخزن شامل یک پیاده‌سازی کامل پایتون از روش امنیت سخت‌افزاری ارائه شده در مقاله IEEE است: **"تشخیص تروجان سخت‌افزاری مبتنی بر یادگیری عمیق با استخراج اطلاعات مبتنی بر بلوک نت‌لیست."**

این پروژه یک خط لوله کامل و پایان به پایان برای آموزش یک مدل یادگیری عمیق فراهم می‌کند که بتواند مدارهای مخرب (تروجان سخت‌افزاری - HT) را با تحلیل ساختار آن‌ها در فایل‌های نت‌لیست سطح گیت شناسایی کند.

**بر اساس مقاله تحقیقاتی:**

س. یو، چ. گو، و. لیو و م. اونیل، "تشخیص تروجان سخت‌افزاری مبتنی بر یادگیری عمیق با استخراج اطلاعات مبتنی بر بلوک نت‌لیست"، IEEE Transactions on Emerging Topics in Computing، 2021.  
DOI: 10.1109/TETC.2021.3116484

## **🧠 مفهوم اصلی: مدارها به عنوان یک زبان**

ایده اساسی این پروژه این است که ساختار چیدمان یک مدار سخت‌افزاری را مانند یک زبان انسانی در نظر بگیریم. این امر به ما امکان می‌دهد از تکنیک‌های قدرتمند پردازش زبان طبیعی (NLP) و یادگیری عمیق استفاده کنیم.

خط لوله به این صورت عمل می‌کند:

1. **فایل (.v) -> گراف:** یک نت‌لیست سطح گیت (فایل وریلوگ) به یک گراف جهت‌دار سطح پین-به-پین با استفاده از networkx تجزیه می‌شود.
2. **گراف -> جملات:** ما یک جستجوی عرض اول (BFS) را از هر گیت تکی (به عنوان یک "مؤلفه مرکزی") انجام می‌دهیم تا تمام مسیرهای سیگنالی که از آن عبور می‌کنند استخراج شوند. هر مسیر به یک "جمله" تبدیل می‌شود (معروف به **ردیابی PCP**).
3. **اتصالات -> کلمات:** هر اتصال در این مسیر (مثلاً *InputPin -> Cell -> OutputPin*) به عنوان یک "کلمه" در نظر گرفته می‌شود (یک **کلمه PCP**).
4. **مدل NLP (Net2Vec):** یک مدل Word2Vec (Skip-gram) روی میلیون‌ها از این "جملات" آموزش داده می‌شود تا یک فرهنگ واژه (net2vec.vectors) ایجاد کند. این فرهنگ واژه هر "کلمه" (اتصال مدار) را به یک بردار 100 بعدی می‌نگاشت که معنای محتوایی آن را نشان می‌دهد.
5. **مدل DL (LSTM):** یک شبکه LSTM (حافظه طولانی کوتاه مدت) روی این جملات برداری شده آموزش داده می‌شود. آن یاد می‌گیرد که "گرامر" و "ساختار" جملات مدارهای عادی را از آنچه در تروجان سخت‌افزاری یافت می‌شود متمایز کند.
6. **رأی‌گیر (شناسایی نهایی):** برای تشخیص تروجان در یک فایل جدید، مدل تمام ردیابی‌ها را اسکن می‌کند. یک ماژول **رأی‌گیر** نتایج را تجمیع می‌کند. اگر گیت خاصی مرکز بسیاری از ردیابی‌های "مشکوک" باشد، به عنوان بخشی از یک تروجان علامت‌گذاری می‌شود.

## **✨ ویژگی‌ها**

* **خط لوله 4 مرحله‌ای کامل:** تمام مراحل توصیف شده در مقاله را پیاده‌سازی می‌کند:
  1. تجزیه و استخراج ویژگی نت‌لیست (الگوریتم 1 و 2)
  2. تعبیه کلمات NLP (Net2Vec)
  3. آموزش تشخیص‌دهنده LSTM
  4. رأی‌گیر سطح مؤلفه برای ارزیابی نهایی
* **تجزیه‌کننده پویا و قوی:** تجزیه‌کننده (detector.py) طوری طراحی شده است که بتواند چندین فرمت نت‌لیست وریلوگ را مدیریت کند، شامل:
  * **نگاشت درگاه** صریح (مثلاً .Q(wireA)) که در مجموعه‌های آموزشی TRIT-TC/TS استفاده می‌شود.
  * **نگاشت درگاه** ضمنی (مثلاً (wireA, wireB)) که در مجموعه‌های استاندارد ISCAS استفاده می‌شود.
* **نرمال‌سازی درگاه:** شامل یک نقشه نرمال‌سازی (NORM_MAP) برای ترجمه نام‌های کتابخانه درگاه مختلف (مثلاً nnd2s1، nand2_1، nand) به یک نشانه کلی (مثلاً NAND2). این امر مدل را قوی‌تر و "کتابخانه-بی‌گروه" می‌کند.
* **بهینه‌سازی برای مجموعه‌داده‌های بزرگ:** بارگیری‌کننده‌های داده (dataset.py) طوری طراحی شده‌اند که بتوانند مجموعه‌داده‌های بزرگ (7 میلیون+ نمونه) را روی ماشین‌های با حافظه RAM کم (16 گیگابایت) مدیریت کنند با بارگیری تمام داده‌ها در حافظه و استفاده از num_workers=0.

## **📂 ساختار پروژه**

```
.
├── 📁 Dataset/                  # حاوی داده‌های آموزش/آزمون
├── 📁 English/                 # فایل‌های پیاده‌سازی و مستندات انگلیسی
│   ├── 🐍 cleaner.py           # ابزارهای تمیزسازی داده
│   ├── 🐍 create_balanced_dataset.py  # ابزارهای تعادل داده
│   ├── 🐍 dataset.py           # پیاده‌سازی مجموعه داده PyTorch
│   ├── 🐍 detector.py          # ابزار شناسایی اصلی برای اسکن نت‌لیست‌ها
│   ├── 🐍 evaluate.py          # ابزارهای ارزیابی مدل
│   ├── 🐍 model.py             # تعریف مدل LSTM
│   ├── 🐍 netlist_parser.py    # ابزارهای تجزیه نت‌لیست
│   ├── 🐍 phase1_graph_utils.py # تبدیل گراف و استخراج ردیابی
│   ├── 🐍 preprocess_nlp.py    # ابزار پیش‌پردازش NLP
│   ├── 🐍 process_originals.py # پردازش طراحی‌های اصلی (بدون تروجان)
│   ├── 🐍 run_batch_extraction.py # پردازش دسته‌جمعی برای استخراج ویژگی
│   ├── 🐍 train_detector_updated.py # آموزش مدل LSTM
│   ├── 🐍 train_nlp.py         # آموزش مدل Net2Vec
│   └── 📄 README.md            # این فایل
├── 📁 jsonl_dataset/           # داده‌های آموزشی با فرمت JSONL
│   ├── corpus_ALL.jsonl        # فرهنگ کامل برای مدل NLP
│   └── labeled_traces_BALANCED.jsonl  # مجموعه داده آموزشی متوازن
├── 📁 Model/                   # مدل‌های آموزش دیده
│   ├── net2vec.vectors         # تعبیه‌های کلمات آموزش دیده
│   └── trojan_detector_final.pth  # تشخیص‌دهنده LSTM آموزش دیده
├── 📁 Persian/                 # فایل‌های پیاده‌سازی فارسی (تکراری از انگلیسی)
│   ├── 🐍 cleaner.py
│   ├── 🐍 create_balanced_dataset.py
│   ├── 🐍 dataset.py
│   ├── 🐍 detector.py
│   ├── 🐍 evaluate.py
│   ├── 🐍 model.py
│   ├── 🐍 netlist_parser.py
│   ├── 🐍 phase1_graph_utils.py
│   ├── 🐍 preprocess_nlp.py
│   ├── 🐍 process_originals.py
│   ├── 🐍 run_batch_extraction.py
│   ├── 🐍 train_detector_updated.py
│   └── 🐍 train_nlp.py
└── 📄 .gitignore               # الگوهای نادیده گرفتن گیت
```

## **🚀 شروع سریع و استفاده**

### **1. نصب**

1. این مخزن را کلون کنید:
   ```bash
   git clone <your-repo-url>
   cd VHDL_AI_Project
   ```

2. یک محیط مجازی پایتون ایجاد کنید و فعال کنید:
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # در ویندوز: .venv\\Scripts\\activate
   ```

3. تمام کتابخانه‌های مورد نیاز را نصب کنید:
   ```bash
   pip install torch torchvision torchaudio
   pip install networkx gensim tqdm numpy
   ```

### **2. گزینه A: آموزش مجدد کل مدل (توصیه می‌شود)**

این خط لوله کامل برای تکرار نتایج مقاله از ابتدا است.

```bash
# === مرحله 1: استخراج ویژگی ===
# (این الگوریتم 1 و 2 را روی تمام فایل‌های آموزشی اجرا می‌کند و فایل‌های .gpickle و .json را ایجاد می‌کند)
# (این مدت زمان طولانی خواهد برد)
echo "--- اجرای مرحله 1 ---"
python English/run_batch_extraction.py
python English/process_originals.py

# === مرحله 2: پردازش زبان طبیعی و آماده‌سازی داده ===
# (این تمام فایل‌های .json را جمع‌آوری کرده و مدل زبانی را آموزش می‌دهد)
echo "--- اجرای مرحله 2 ---"
python English/preprocess_nlp.py
python English/train_nlp.py
python English/create_balanced_dataset.py

# === مرحله 3: آموزش تشخیص‌دهنده ===
# (این از GPU شما برای آموزش LSTM استفاده می‌کند)
echo "--- اجرای مرحله 3 ---"
python English/train_detector.py

# === مرحله 4: ارزیابی مدل ===
# (این رأی‌گیر را اجرا کرده و معیارهای دقت نهایی را برای شما ارائه می‌دهد)
echo "--- اجرای مرحله 4 ---"
python English/evaluate.py
```

### **3. گزینه B: استفاده از مدل آموزش دیده (ابزار شناسایی)**

پس از داشتن net2vec.vectors و trojan_detector_final.pth، می‌توانید از detector.py به عنوان یک ابزار مستقل برای اسکن فایل‌های نت‌لیست جدید استفاده کنید.

**استفاده:**

```bash
python English/detector.py <path_to_your_netlist.v>
```

**مثال (اسکن یک فایل شناخته شده عادی):**

```bash
python English/detector.py Test/s713.v
```

```
--- 🔬 فاز 1: پردازش s713.v ---
  (1/3) 📄 تجزیه نت‌لیست (حالت پویا)...
  (2/3) 🧱 تولید بلوک‌ها (الگوریتم 1): 100%|...| 412/412
  (3/3) 💬 استخراج ردیابی‌ها (الگوریتم 2): 100%|...| 412/412
✅ فاز 1 به پایان رسید. 1,095 ردیابی از 412 گیت استخراج شد.

--- 🧠 فاز 3: بارگیری مدل‌ها ---
  (استفاده از دستگاه: cuda)
  ✅ فرهنگ Net2Vec (بردارها) بارگیری شد.
  ✅ مدل تشخیص تروجان (trojan_detector_final.pth) بارگیری شد.

--- 🤖 فاز 4: اجرای استنتاج و رأی‌گیری ---
  (1/2) 🧠 اجرای استنتاج: 100%|...| 9/9
  (2/2) 🗳️ فرآیند رأی‌گیری: 100%|...| 412/412

==================================================
🏁 اسکن کامل شد
==================================================
  ✅ نتیجه: هیچ تروجان سخت‌افزاری در این فایل پیدا نشد.

⏱️ زمان کل اسکن: 12.52 ثانیه
```

**مثال (اسکن یک فایل شناخته شده بد):**

```bash
python English/detector.py Test/s38417_T0099_C.v
```

```
--- 🔬 فاز 1: پردازش s38417_T0099_C.v ---
  ... (تجزیه و استخراج) ...
✅ فاز 1 به پایان رسید. بیش از 10,000 ردیابی استخراج شد.
...
--- 🤖 فاز 4: اجرای استنتاج و رأی‌گیری ---
  ... (استنتاج و رأی‌گیری) ...

==================================================
🏁 اسکن کامل شد
==================================================
  🚨 هشدار: 4 گیت مشکوک به تروجان پیدا شد!
--------------------------------------------------
  لیست گیت‌های مشکوک:
    1. troj49_0_U1
    2. troj49_0_U2
    3. troj49_0_U3
    4. troj49_0_U4

⏱️ زمان کل اسکن: 14.20 ثانیه
```

## **📚 منابع**

* **مقاله اصلی:** س. یو، و دیگران. "تشخیص تروجان سخت‌افزاری مبتنی بر یادگیری عمیق با استخراج اطلاعات مبتنی بر بلوک نت‌لیست." *IEEE TETC*، 2021.
* **داده‌های مرجع:** ج. کروز، و دیگران. "یک چارچوب قابل پیکربندی خودکار برای وارد کردن تروجان پویا در مجموعه داده‌های اعتماد" *DATE*، 2018. (ارائه شده توسط [Trust-Hub](https://trust-hub.org/))

## **📄 مجوز**

این پروژه تحت مجوز MIT منتشر شده است - برای جزئیات بیشتر فایل مجوز را ببینید.